#include <iostream>
using namespace std;

int main () {
    //  int a = 3 , b = 7;
    cout << (4 << 1) << endl;  // 4 is bianry form  // 1 is shift position   and shift 1 place in left side
    cout << ( 10 << 2 ) << endl; // 10 is binary number first convert then shift 2 place 

     return 0;
}
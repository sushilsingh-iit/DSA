#include <iostream> // Preprocessor directive to include I/O functions
using namespace std;

int main() {
    // Standard output stream
    cout << ((2>1) && (3>1)) << endl; // every condition ar etrue then give true 
    
    return 0; // Signals that the program finished successfully
}